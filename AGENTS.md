# 项目协作约束

回答除专业名词外尽量中文且简洁，用自然段表达，不用序号/嵌套结构；避免“不是..而是..”“不仅...而且..”，不用比喻、无用比较、夸张。

PowerShell/Windows 禁止 python here-doc；使用 python -c 或脚本。默认 Python 环境 conda myenv；本机 rg 被拒绝执行，使用 PowerShell 搜索。随机实验统一设置 seed，必要时 deterministic；生产唯一身份生成不改为固定随机序列。训练脚本须冒烟到训练循环或一个 epoch 后才能报告已开跑。长循环展示进度、参数、完成比例、结束时间与预计完成时间。

Kotlin 优先于 Java，使用中文 KDoc、类型安全实现，禁止空断言及 @Suppress 掩盖类型问题。Python 使用中文注释，尽量减少其他语言。若绘制流程图，必须使用 design-doc-mermaid 和 mermaid-skill；全黑中文字体、正交连接、零重叠、连线连接边缘、边距均匀、说明文字与线条分离。

本地代理 localhost:7897；JDK 位于 D:/.jdks；默认新项目目录 D:/workspace。

## 恢复工作

开始或恢复重构前必读 docs/refactor/PLAN.md、FILES.csv、BEHAVIOR.md、HANDOFF.md，然后核对 Git 差异、实际源码和验证版本。每个可验证小批次结束立即更新记录。文件状态只能使用：待审查、待重构、进行中、待验证、已验证、已审查保留、阻塞。未经行为验证不得仅凭编译标记已验证；后续影响需重新验证。

保持公开接口、线协议、配置和数据兼容；业务语义变化先记录并澄清。保护本地修改，不覆盖远程变更。未完成项必须明确报告，不宣称整体完成。
